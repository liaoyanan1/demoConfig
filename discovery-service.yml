eureka:
  instance:
    hostname: 10.18.8.231
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/,http://${eureka.instance.hostname}:8762/eureka/
    register-with-eureka: false
    fetch-registry: false
spring:
  security:
    user:
      name: lyn
      password: 123456
    
